"""Ядро дома: всё, что не зависит от Home Assistant.

HA — один из адаптеров (`docs/plan-core-without-ha.md` в менеджере): он стоит
уровнем выше (`custom_components/mega_home/*.py`) и даёт ядру хозяина среды
(`host.py`) и источник состояний (`source.py`). Самостоятельный демон возьмёт
этот каталог как есть и положит рядом свой адаптер.

⚠ Ядро едет ВНУТРИ интеграции, а не отдельным пакетом из PyPI: HACS ставит
только этот каталог, объекты бывают без интернета, а две версии (ядра и
адаптера) расходились бы молча. Граница держится замком
`tests/test_core_without_ha.py`: ни `homeassistant`, ни импорта из адаптера.
"""
